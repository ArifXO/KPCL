# @package _global_.model
type: mlp
hidden_dim: null      # null => auto-matched per input_dim to KAN params (R1, <=15%)
embed_dim: 64         # head output = final contrastive embedding dim
n_enc_layers: 2
use_layer_norm: true  # MLP twin WITH parameter-free LayerNorm matching the KAN fix (fair control)
weight_decay: 1.0e-4
lr: 1.0e-3
# Same param count as mlp.yaml (LayerNorm is parameter-free) -> R1 parity intact.
